Что было что есть.
Что могу и хочу добавить/переработать.

К 10 в субботу обсуждаем.

# UI для создания/редактирования задач

Его не было. Создавать и редактировать предлагалось через IntelliJ IDEA и пушить в git,
где уже есть ветки, коммиты, история и тд. Есть разграничение доступа и подмодули.
Решение имеет право на жизнь, но не здесь. Здесь нужно написать UI.

# История изменений задач

Изменения атрибутов, тестов, тегов и тд.

# Добавление языков через плагинизацию

Разработать SDK, чтобы потребитель мог разрабатывать поддержку новых языков.
Интерфейсы

# Ограничение конкурентности запускаемых тестов

Сейчас рабочие узлы подхватывают записи из кафки, сразу и запускают выполнение на себе через Docker Engine API.
Нужно ограничить по числу CPU на машине.
4 ядра = 4 одновременных запуска. считаем, что хост приложение не потребляет.

# Заново продумать архитектуру

Может класть в кафку это лишнее?

Пользователи шлют код на проверку. Можно ли сравнить запрос на проверку кода с денежной транзакцией, которую обязательно
нужно выполнить?
Или можно отказать, сказав что пока нет компьюта.

**task-runner не масштабируется**
