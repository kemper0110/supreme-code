Начало не придумал.

Начать с того, что платформа позволяет писать код?

# Добавил LSP

Можно использовать полноценный автокомплит и проверку типов при наборе кода. 
В литкоде и кодворс лишь слабая подсветка.
Можно подключить библиотеки. Например, для python подключены pandas и numpy. 
Если есть pandas и numpy, то можно реализовать первую лабораторную по ИИ с изучением этих библиотек. 

# Создание задачи

Написал простенькое описание задачи.
Дальше нужно написать тесты, эталонное решение, шаблон для решения.

Или не нужно? 
Не нужно.

# Генерация через AI

Подключаем кодингового агента к работе. У меня это opencode. Пользователь может выбрать любого другого. 
Пишем простенький промпт: "Напиши реализацию задачи на языке python по существующему описанию."

Получили тесты и решение на языке python. **Немного описать** 
У вас мог возникнуть вопрос: "Как агент догадался, в каком формате должна быть задача, тесты и т.д.?"
Это скилл. Там всё написано. 
Как агент провалидировал? Через MCP сервер. Там он вызвал валидацию и получил ответ. 

Задачу создали. Теперь можно приступить к ее решению.
Пишем код, отправляем на проверку, смотрим, в каких тестах решение не проходит. 

> Добавить образ для python с pytest

Отображаем PyTest 

Конфиг платформы:
- Название класса тестера: метод verdict
- languageId
- imageName
- testsPath
- solutionPath
- reportPath

Дальше лайтовые фичи...

# Прикрутил Keycloak

Раньше была самописная авторизация и без ролей, потому что не нужно было.
Реализовывать авторизацию руками чревато проблемам с безопасностью.
Заменил на проверенный временем опенсорсный сервис авторизации,
потому что авторизация место уязвимое.

Каждый эндпоинт защищен своей привилегией. 
Показать jwt токен, скоупы. Показать работу с ролями/скоупами пользователей. 

# Показать схему сервисов

Придется показать схему всех сервисов, чтобы дальше рассказывать фичи.

# Репликация сервисов

Работа уровня магистратуры должна обеспечивать некоторый уровень надежности.
Система должна быть надежной и доступной.
Для доступности необходимо, чтобы при падении одного инстанса сервиса, система не стала недоступной.
Как бонус, это повышает количество обратываемых запросов системой.

Для репликации сервиса web-server добавил Redis с общим состоянием подписок на получение результата.
Сервис test-runner и ранее позволял репликацию.

> Для остальных сервисов, что сделал?
> Достаточно ли просто проставлять метки на контейнеры?

# Плагинизация

Работа уровня магистратуры должна обеспечивать расширение без изменения ядра приложения. 
Поэтому я добавил плагинизацию. 

# Запуск решений в докере

Очевидно пользовательский код является недоверенным, поэтому просто так запускать его опасно.
Запуск происходит в докере. Безопасно. Подробнее в моей статье, там же есть и сравнение с другими платформами.

# Наблюдаемость

Работа уровня магистратуры должна обеспечивать сопровождаемость. 

## Метрики

Показать JVM метрики, kafka.
Показать бизнес метрики.

> Добавить бизнес метрики.

Отображение нагрузки в разрезе отдельных операций. 

## Трассировка запросов

Показать обработку запроса на проверку решения.
Запрос приходит по http, проходит через кафку, проверяется решение, возвращается по кафку.
Протоколы: Http, kafka, jdbc.
Frontend->Web
Web->postgres
Web->minio
Web->kafka
kafka->test-runner
test-runner->minio
test-runner->docker-engine
test-runner->kafka

Пример поиска задержки. Тормознутный плагин. 

## Централизованный сбор логов

> Воткнуть логи. Показать логи по всему трейсу. 