Тезисы.

Как масштабировать.
По мере увеличения очереди добавлять test-runner.

Очередь Job в кубере. Как у знать
LSP.

Цель диплома - вопрос взят на карандаш.

Яндекс контест.

# Как масштабировать

> Проблема, которую вы описали, действительно существует: при использовании Kafka максимальный параллелизм обработки
> сообщений в рамках одной группы потребителей ограничен количеством партиций топика. Если вы хотите динамически
> масштабировать число экземпляров test-runner (подов) в зависимости от нагрузки, то при превышении количества подов над
> партициями часть из них будет простаивать. Увеличение числа партиций возможно, но это необратимая операция (уменьшить
> нельзя без пересоздания топика), и она требует осторожности, особенно если важен порядок обработки сообщений. Кроме
> того, автоматическое масштабирование партиций вместе с подами — нетривиальная задача, которую обычно не решают
> встроенными средствами.

1 проверка = 1 CPU
200 проверок = 200 CPU
1 проверка = до 12 секунд
RPS = 200 / 12 = 16

Нагрузка на очередь маленькая.
Долговременно хранить сообщения не нужно. Повторно обрабатывать не нужно.
Порядок проверки задач мне не важен.
В RabbitMQ не нужно масштабировать партиции топика.

# LSP

**LSP передает только диффы файлов.**

Получается три копии файла:

- В браузере пользователя (Monaco)
- На диске сервера (временная директория)
- В памяти LSP сервера

**Как масштабировать.**
один пользователь = один процесс LSP (как минимум). LSP серверы тяжелые и stateful.

StatefulSet для LSP (нужен стабильный network identity)
Redis для хранения маппинга user → LSP
Sticky sessions через IP hash или cookie
HPA по памяти, но с учетом специфики (max 1 user per pod)
Автоудаление неактивных LSP через 30 минут
Resource limits обязательны (иначе один пользователь положит весь кластер)